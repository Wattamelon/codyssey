1. 도커 이미지 
```bash
    (base) melon@munseongon-ui-MacBookAir codyssey % docker images
    REPOSITORY   TAG       IMAGE ID   CREATED   SIZE 
    ```

2. 도커 컨테이너
```bash
    (base) melon@munseongon-ui-MacBookAir codyssey % docker ps
    CONTAINER ID   IMAGE     COMMAND   CREATED   STATUS    PORTS     NAMES
    (base) melon@munseongon-ui-MacBookAir codyssey % docker ps -a
    CONTAINER ID   IMAGE     COMMAND   CREATED   STATUS    PORTS     NAMES
    (base) melon@munseongon-ui-MacBookAir codyssey % 

```